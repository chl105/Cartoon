<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.sltpaya.cartoon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.sltpaya.tool.Utils$ToolApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup,android:supportsRtl,android:label,android:icon">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.StripManDetailActivity" />
        <activity android:name=".activity.BookNewDetailsActivity" />
        <activity android:name=".activity.CartoonRankActivity" />
        <activity android:name=".activity.BookMoreActivity" />
        <activity android:name=".activity.ClassifyActivity" />
        <activity android:name=".activity.NovelRankActivity" />
        <activity android:name=".activity.NovelDetailActivity" />
        <activity android:name=".activity.RednecksRankActivity"></activity>
    </application>

</manifest>